<%= search_form_for q, url: url do |f| %>
  <div class="input-group mb-3">
    <%= f.search_field :title_or_body_cont, class: "form-control", plaeholder: t('defaults.search_word') %>
        <%= f.submit "検索", class: "btn btn-primary" %>
      </div>
  </div>
<% end %>